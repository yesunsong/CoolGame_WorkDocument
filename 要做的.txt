equipstrengthwindow 需要修改

nalist的 到头尾的事件

把 奖励作为缓存储存起来，使用命令接口

sprite增加  上下两层容器

特定窗口出现时，一些 活动窗口 暂时不出现。

强化的问题 muhontype

爬塔的回调函数

特效的基类可以抽象成 工厂模式

无资源按钮转为 有资源按钮 不行

uicontainer不支持 设置 -3 -2，默认从-1开始——okchogn

背包的aloadingbar显示有问题

检查安卓权限